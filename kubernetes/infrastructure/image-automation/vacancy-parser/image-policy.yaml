apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: vacancy-parser
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: vacancy-parser
  policy:
    semver:
      range: '>=0.0.1'